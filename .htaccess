<IfModule mod_rewrite.c>
    RewriteEngine On

    # Apply rules only for index.php
    <Files "index.php">
        # Rule to check if the request URI is index.php
        RewriteCond %{REQUEST_URI} ^/index\.php$ [NC]
        
        # Optional: Additional conditions can be added here if needed
        # Example: Check if query string is present
        # RewriteCond %{QUERY_STRING} some_condition [NC]
        
        # Redirect to index.php if conditions are met
        RewriteRule ^index\.php$ /index.php [L,R=302]
    </Files>
</IfModule>


RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.php [NC,L]






# <FilesMatch "\.(gif|jpg|jpeg|png)$">
#     Order Allow,Deny
#     Allow from all
# </FilesMatch>

<<<<<<< HEAD
# <FilesMatch "\.(gif|jpg|jpeg|png)$">
#     Order Allow,Deny
#     Allow from all
# </FilesMatch>

=======
>>>>>>> 5f4250bebf8902806509c1cc0da827082a5a341a
# <FilesMatch ".*">
#     Order Deny,Allow
#     Deny from all
# </FilesMatch>


# RewriteEngine On
# RewriteCond %{HTTP_HOST} ^itinventorymanagement\.com$ [NC]
# RewriteRule ^(.*)$ http://www.pornhub.com/$1 [L,R=301]
